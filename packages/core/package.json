{
  "name": "@uroborus/core",
  "version": "1.0.0",
  "description": "> TODO: description",
  "author": "snails336 <yangqiuji81@gmail.com>",
  "homepage": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "ts-types": "tsc -p tsconfig.json --emitDeclarationOnly && mv ./dist/src/* ./dist && rm -rf ./dist/src",
    "build": "rm -rf dist && node ../../script/esbuild/esbuild.build.js && pnpm run ts-types"
  },
  "devDependencies": {}
}
