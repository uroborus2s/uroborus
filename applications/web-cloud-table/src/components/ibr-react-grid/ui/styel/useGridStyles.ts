import makeStyles from '@mui/styles/makeStyles';

const useGridStyles = makeStyles({
  rootWrapper: {
    height: '100%',
    width: '100%',
    position: 'relative',
    userSelect: 'none',
  },
  keyboardFocusClass: (props) => ({}),
});

export default useGridStyles;
